# Generated by aleph.nativelink module
# Dedicated nix builder - SSH in, build containers, push to registry
# Your laptop stays cool.
app = "@appPrefix@-builder"
primary_region = "@region@"

[build]
  image = "registry.fly.io/@appPrefix@-builder:latest"

[env]
  NIX_CONFIG = "experimental-features = nix-command flakes"

[mounts]
  source = "builder_nix"
  destination = "/nix"
  initial_size = "@builderVolumeSize@"

[[vm]]
  memory = "@builderMemory@"
  cpu_kind = "performance"
  cpus = @builderCpus@
